% This script performs phase-amplitude coupling (PAC) analysis of sleep
% spindle events, comparing events with high and low slow-oscillation
% amplitude ratios, and extracts PAC phase and strength features across sessions.

clc 
clear
close all

%% 
% % load thal_ch_norm_filtered_ChooseOneChannelFromEachShanks.mat
% load SWS_episode.mat
% load SOdetectedForChooseOneChannelFromEachShank.mat
% load SOreferenceChannelDelayAndRatio.mat
paths = dir('*/firing rate spindles*');
[nRawspath, ~] = size(paths);

fs = 1250;
SessionNum = 18;
MeanPacPhaseBig = zeros(1,SessionNum);
MeanPacPhaseshort = zeros(1,SessionNum);
medianPacStrengthBig= zeros(1,SessionNum);
medianPacStrengthShort= zeros(1,SessionNum);
%%
        path = "D:\daniel\PAC figures";
nbin=50;
 
% [y,IsBlip] = SOMolle(thal_ch_norm_filtered_ChooseOneChannelFromEachShanks(1,:));
for i=1:1%SessionNum
    clc
    display( ['session is = ' num2str(i)])

    load (['SWS_episode' num2str(i) '.mat'])
    load(['SOdetected' num2str(i) '.mat'])
    load (['refSochannels' num2str(i) '.mat' ])
    load (['SOreferenceChannelDelayAndRatioFrom0.5to4Hz' num2str(i) '.mat'])
    %% for all events 
%     pac_all = PAC(1, [-2.5 -2], false, true);
%     
%     data = SWS_episode(1,:);
%     event_sample = find(SOdetected.ISBLIPE(1, :) == 1);
%     
%     pac_all.Run(data, [7 15], data, [0.5 4], event_sample, fs);
%     
%     % plot(pac_all)
%     
%     figure
%     imagesc(pac_all.Time, pac_all.Freq, pac_all.MeanTFR);
%     set(gca,'YDir', 'normal');
%     hold on;
%     plot(pac_all.Time, pac_all.MeanData/max(pac_all.MeanData)*2+11, 'k', 'LineWidth', 2);
%     title(['PAC for all events (session)=' num2str(i)],'Color','k')
%     xlabel('Time (s)');
%     ylabel('Freq');
%     set(gca,'fontsize',16,'color',[0 0 0])
%     set(groot, 'defaultFigureColor', [0 0 0]);
%     set(groot, 'defaultAxesXColor', [1 1 1], 'defaultAxesYColor', [1 1 1], 'defaultAxesZColor', [1 1 1]);
%     
%     figure
%     circ_plot(phase(pac_all.SI), 'hist', [], 20,true,true,'linewidth',2,'color','r')
%     set(gca,'fontsize',16,'color',[0 0 0])
%     set(groot, 'defaultFigureColor', [0 0 0]);
%     set(groot, 'defaultAxesXColor', [1 1 1], 'defaultAxesYColor', [1 1 1], 'defaultAxesZColor', [1 1 1]);
%     title(['angle for all events (session) = ' num2str(i) ],'Color','k')
%     
%     figure
%     histogram(abs(pac_all.SI),'FaceColor','b')
%     title('strength for all events ','Color','k')
%     set(gca,'fontsize',16,'color',[0 0 0])
%     set(groot, 'defaultFigureColor', [1 1 1 ]);
%     set(groot, 'defaultAxesXColor', [1 1 1], 'defaultAxesYColor', [1 1 1], 'defaultAxesZColor', [1 1 1]);
%     
    %% events with biger ratio
    pac_big = PAC(1, [-2.5 -2], false, true);
    
    data = SWS_episode.SWS_episode  (refSochannels.refCh ,:);
    indexBig = find(SOreferenceChannelDelayAndRatio.ratio (1, :) > 0.5);
    event_sample = find(SOdetected.ISBLIPE(refSochannels.refCh, :) == 1);

    event_sample_big = event_sample(indexBig);
    
    pac_big.Run(data, [7 15], data, [0.5 4], event_sample_big, fs);

    figure(i)
    
    subplot(3,2,1)
    
    imagesc(pac_big.Time, pac_big.Freq, pac_big.MeanTFR);
    set(gca,'YDir', 'normal');
    hold on 
    plot(pac_big.Time, pac_big.MeanData/max(pac_big.MeanData)*2+11, 'LineWidth', 2,'Color','k');
    
    xlabel('Time (s)');
    ylabel('Freq');
%     set(gca,'fontsize',16)
%     set(groot, 'defaultFigureColor', [1 1 1]);
%     set(groot, 'defaultAxesXColor', [0 0 0], 'defaultAxesYColor', [0 0 0], 'defaultAxesZColor', [0 0 0]);
    title(['PAC for double tops session = ' num2str(i)])
    
    
    subplot(3,2,3)
    circ_plot(phase(pac_big.SI), 'hist', [], 20,true,true,'linewidth',2,'color','b')
        title ('angle for double tops')

%     set(gca,'fontsize',16)
%     set(groot, 'defaultFigureColor', [1 1 1]);
%     set(groot, 'defaultAxesXColor', [0 0 0], 'defaultAxesYColor', [0 0 0], 'defaultAxesZColor', [0 0 0]);title (['angle for big events session = ' num2str(i)])
%     
    subplot(3,2,5)
    histogram(abs(pac_big.SI),nbin,'FaceColor','b')
    title(['strength for double tops session ' num2str(i)])
    
%     set(gca,'fontsize',16)
%     set(groot, 'defaultFigureColor', [1 1 1]);
%     set(groot, 'defaultAxesXColor', [0 0 0], 'defaultAxesYColor', [0 0 0], 'defaultAxesZColor', [0 0 0]);    
    %% events with shorter ratio
    pac_short = PAC(1, [-2.5 -2], false, true);
    
%     data = SWS_episode(1,:);
    indexShort= find(SOreferenceChannelDelayAndRatio.ratio(1, :) <= 0.5);
    event_sample_short = event_sample(indexShort);
    
    pac_short.Run(data, [7 15], data, [0.5 4], event_sample_short, fs);
    
    subplot(3,2,2)
    imagesc(pac_short.Time, pac_short.Freq, pac_short.MeanTFR);
    set(gca,'YDir', 'normal');
        hold on 

    plot(pac_short.Time, pac_short.MeanData/max(pac_short.MeanData)*2+11, 'k', 'LineWidth', 2);
    xlabel('Time (s)');
    ylabel('Freq');
    set(gca,'fontsize',16)
    set(groot, 'defaultFigureColor', [1 1 1]);
    set(groot, 'defaultAxesXColor', [0 0 0], 'defaultAxesYColor', [0 0 0], 'defaultAxesZColor', [0 0 0]);
    title (['PAC for single top session = ' num2str(i)])
    
    subplot(3,2,4)
    circ_plot(phase(pac_short.SI), 'hist', [], 20,true,true,'linewidth',2,'color','r')
    set(gca,'fontsize',16)
    set(groot, 'defaultFigureColor', [1 1 1]);
    set(groot, 'defaultAxesXColor', [0 0 0], 'defaultAxesYColor', [0 0 0], 'defaultAxesZColor', [0 0 0]);
    title ('angle for single top')
    
    subplot(3,2,6)
    histogram(abs(pac_short.SI),nbin,'FaceColor','r')
    title(['strength for single top session ' num2str(i)])
%     set(gca,'fontsize',16)
%     set(groot, 'defaultFigureColor', [1 1 1]);
%     set(groot, 'defaultAxesXColor', [0 0 0], 'defaultAxesYColor', [0 0 0], 'defaultAxesZColor', [0 0 0]);

    %%
    PAC_features.angleShort = phase(pac_short.SI);
    PAC_features.strengthShort = abs(pac_short.SI);
    PAC_features.angleBig = phase(pac_big.SI);
    PAC_features.strengthBig = abs(pac_big.SI);
%     PAC_features.angleAll = phase(pac_all.SI);
%     PAC_features.strengthALl = abs(pac_all.SI);
    save (['PAC_features' num2str(i) '.mat'] ,'PAC_features')
    MeanPacPhaseBig(i) = mean(PAC_features.angleBig);
    MeanPacPhaseshort(i) = mean(PAC_features.angleShort);
    medianPacStrengthBig(i) = median(PAC_features.strengthBig);
    medianPacStrengthShort(i) = median(PAC_features.strengthShort);


end
PAC_featuresAll.MeanPacPhaseBig = MeanPacPhaseBig;
PAC_featuresAll.MeanPacPhaseshort = MeanPacPhaseshort;
PAC_featuresAll.medianPacStrengthBig = medianPacStrengthBig;
PAC_featuresAll.medianPacStrengthShort = medianPacStrengthShort;
save('PAC_featuresAll','PAC_featuresAll')
%       save figures

        
        
%         Get handles of all open figures
        figures = findobj('Type','figure');
                        
%         Maximize each figure
        for n = 1:length(figures)
            set(figures(n),'WindowState','maximized');
        end
        
%         Get the path to the directory where you want to save the figures
        save_path = (path);
        
%         Get a list of all open figures
        fig_list = findobj('Type', 'figure');
        
%         Save each figure to the specified path
        for n= 1:length(fig_list)
            fig = fig_list(n);
            saveas(fig, fullfile(save_path, sprintf('figure_%d.png', n)));
                saveas(fig, fullfile(save_path, sprintf('figure_%d.fig', n)));
        
        end
