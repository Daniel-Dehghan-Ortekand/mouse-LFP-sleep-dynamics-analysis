%% Data organization and preprocessing
% This script loads spindle detection results from individual thalamic
% channels and reorganizes them into a single cell array, where each cell
% contains the spindle information for one channel. The resulting
% channel-wise spindle data are saved as a single MAT file for subsequent
% spindle detection and coupling analyses.

clc 
clear 
close all 
%%

% pathsthal = dir('*/thal_spindles_ch*');


for k=1:62
% 
%     filename = strcat(pathsthal(k).folder, filesep, pathsthal(k).name); % Use filesep instead of just '/'
%     data = load(filename);
       data= load(['thal_spindles_140822_ch' num2str(k)]); 
      thal_spindles{k} = data;
%     save(['thal_spindles' num2str(k)], 'thal_spindles', '-v7.3');
%         clear data

end
save thal_spindles18.mat thal_spindles
%%

